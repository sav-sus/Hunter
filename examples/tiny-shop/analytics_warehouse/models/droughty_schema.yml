# Generated by droughty. Do not edit by hand.
#
# Regenerate with `droughty dbt`, then check this file against the
# test_overwrite block in droughty_project.yaml: overrides have been silently
# dropped before now.
version: 2
models:
  - name: wh_commerce__order_fact
    columns:
      - name: order_pk
        description: '{{doc("order_pk")}}'
        tests:
          - not_null
          - unique
      - name: customer_fk
        description: '{{doc("customer_fk")}}'
        tests:
          - dbt_utils.at_least_one
      - name: order_natural_key
        description: '{{doc("order_natural_key")}}'
        tests:
          - dbt_utils.at_least_one
      - name: order_total_amount
        description: '{{doc("order_total_amount")}}'
        tests:
          - dbt_utils.at_least_one
  - name: wh_master__customer_dim
    columns:
      - name: customer_pk
        description: '{{doc("customer_pk")}}'
        tests:
          - unique
      - name: customer_name
        description: '{{doc("customer_name")}}'
        tests:
          - dbt_utils.at_least_one
  - name: wh_master__product_dim
    columns:
      - name: product_pk
        description: '{{doc("product_pk")}}'
        tests:
          - not_null
          - unique
