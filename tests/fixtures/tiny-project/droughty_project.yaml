profile: tiny_shop

dbt_path: models/
field_description_path: models
field_description_file_name: field_descriptions.md
dbt_tests_filename: droughty_schema

test_overwrite:
  models:
    wh_shop__order_fact:
      order_pk:
        - not_null
        - unique
    wh_shop__customer_dim:
      customer_pk:
        - unique
        - not_null
